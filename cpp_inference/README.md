# C++ Inference Core

This directory will contain the GPU inference core implementation.

The initial version will start with a minimal stub, then evolve into a real GPU-backed inference engine using CUDA, TensorRT, or ONNX Runtime.
